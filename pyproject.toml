[project]
name = "wraithvector"
version = "0.1.0"
description = "Runtime governance for AI agents"
authors = [{name="WraithVector"}]
dependencies = [
  "requests",
  "python-dotenv"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"